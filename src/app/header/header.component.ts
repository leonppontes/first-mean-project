import {Component} from "@angular/core";

@Component ({
  selector: 'app-header', //CSS Selector - A selector instructs Angular to instantiate this component wherever it finds the corresponding tag in template HTML.
  templateUrl: './header.component.html'
})

export class HeaderComponent {}
